// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

contract ForceFeed {
    /**
     * @dev FIXED: Using >= instead of ==.
     * Even if an attacker forces ETH into this contract via selfdestruct,
     * the logic will not "brick" because we check for a minimum balance.
     */
    function withdrawBonus() public {
        require(address(this).balance >= 7 ether, "Not enough ETH to withdraw");
        // Proceed with withdrawal logic...
    }

    // Function to allow the contract to receive ETH normally
    receive() external payable {}
}
